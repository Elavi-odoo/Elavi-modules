<odoo>
    <data>
        <record id="action_send_winbooks_email" model="ir.actions.server">
            <field name="name">Send to WinBooks</field>
            <field name="model_id" ref="account.model_account_move"/>
            <field name="binding_model_id" ref="account.model_account_move"/>
            <field name="state">code</field>
            <field name="code">
                action = records.action_send_winbooks_email()
            </field>
        </record>
    </data>
</odoo>
